# Tool-Based Memory Example

This is the smallest end-to-end NAT example for the plugin.

Set `NVIDIA_API_KEY`, start Redis Agent Memory Server, then run:

```bash
uv run nat run --config_file examples/tool_memory/configs/config.yml
```
